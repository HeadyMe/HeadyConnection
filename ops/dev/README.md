# Dev Operations (DDEV)

Use DDEV for local Drupal workflows. The repo includes `.ddev/config.yaml` and a helper script at `scripts/dev/bootstrap_ddev.sh`.

## Quick Start
```bash
make dev-bootstrap
```
