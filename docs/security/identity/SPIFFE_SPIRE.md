# SPIFFE/SPIRE Identity Plane

This repo uses SPIFFE/SPIRE for workload identity scaffolding.

## Trust Domain
The trust domain is currently set to `example.heady` in `ops/spire`.
Update this value once the canonical domain is finalized and documented.

## Files
- `ops/spire/server/server.conf`
- `ops/spire/agent/agent.conf`
- `ops/spire/register_workloads.sh`
